export type User = {
  id?: string;
  username: string;
  userId: string;
  email: string;
  firstName?: string,
  lastName?: string,
  avatarPath?: string,
  createdAt?: string;
  password: string;
};
